@model FinancialApplication.Models.CodeFirst.Budget

<!-- Page content-->
<div class="content-wrapper">
    <h3>
        Edit this budget
    </h3>
    <div class="container-fluid">
        <!-- START row-->
        <div class="row">
            <div class="col-md-4">
                <!-- START panel-->
                <div class="panel panel-default no-header-danger">
                    <div class="panel-heading">Enter New Budget Starting Amount</div>
                    <div class="panel-body">
                        @using (Html.BeginForm())
                        {
                            @Html.AntiForgeryToken()
                            @Html.HiddenFor(model => model.Id)
                            @Html.HiddenFor(model => model.HouseholdId)
                            @Html.HiddenFor(model => model.BudgetCategoryId)
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                            <div class="form-group">
                                <label>Budget Amount</label>
                                <div class="input-group m-b">
                                    <span class="input-group-addon">$</span>
                                    @Html.EditorFor(model => model.Amount, new { htmlAttributes = new { @class = "form-control", id = "currency" } })
                                    @Html.ValidationMessageFor(model => model.Amount, "", new { @class = "text-danger" })
                                </div>
                            </div>
                            <button type="submit" class="btn btn-sm btn-default">Save</button>
                        }
                    </div>
                    <div class="panel-footer">
                        @Html.ActionLink("Back to Budgets", "Index", null, new { @class = "btn btn-xs btn-success" })
                    </div>
                </div>
                <!-- END panel-->
            </div>
        </div>
    </div>
</div>

@section Scripts{
    <script src="~/Scripts/jquery.maskMoney.js"></script>
    <script>
        $(function() {
            $('#currency').maskMoney({thousands:""});
        })
    </script>
}
